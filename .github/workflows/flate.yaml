---
name: Flate

on:
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  test:
    name: Flate Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup flate
        uses: home-operations/flate/action@main
        with:
          cache: true

      - name: Run flate test
        run: flate test all --path ./clusters/prod

  diff:
    name: Flate Diff
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup flate
        uses: home-operations/flate/action@main
        with:
          cache: true

      - name: Run flate diff
        run: >-
          flate diff all
          --path ./clusters/prod
          --output diff
          > diff.patch

      - name: Generate Diff Summary
        run: |
          touch diff.patch
          cat diff.patch
          if [ -s diff.patch ]; then
            {
              echo "### Flate Diff"
              echo '```diff'
              cat diff.patch
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
          else
            echo "No diff output" >> "$GITHUB_STEP_SUMMARY"
          fi

