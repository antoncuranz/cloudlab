---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: envoy-gateway-crds
  namespace: networking
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: envoy-gateway-crds
  values:
    crds:
      gatewayAPI:
        enabled: true
      envoyGateway:
        enabled: true
